{% extends 'base.html.twig' %}

{% block title %} Page Home {% endblock %}

{% block body %}

    <a href="{{ path('app_register') }}" class="btn btn-cactus-primary">Create new</a>
    <a href="{{ path('app_login') }}" class="btn btn-cactus-primary">Login</a>
    <a href="{{ path('app_activity_index') }}" class="btn btn-cactus-primary">Activity</a>



    {% if app.user %}
        <a href="{{ path('app_user_show', {'username': app.user.username}) }}" >My Account</a>
    {% endif %}

{% endblock %}
